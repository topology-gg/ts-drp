import { AddMulDRP } from "@topology-foundation/blueprints";
import { ActionType } from "@ts-drp/object";
import { beforeEach, describe, expect, test } from "vitest";


describe("Test ActionTypes", () => {
	beforeEach(() => {
	});

	test("Test: Nop", () => {
	});

	test("Test: DropLeft", () => {
	});

	test("Test: DropRight", () => {
	});

	test("Test: Swap", () => {
		const drp = new AddMulDRP();
	});

	test("Test: Drop", () => {
	});
});
